CREATE TABLE properties(
id SERIAL PRIMARY KEY,
name VARCHAR, 
address VARCHAR, 
city VARCHAR, 
state VARCHAR, 
zip INT
image_url VARCHAR,
monthly_mortgage_amount FLOAT,
desired_monthly_rent FLOAT,
);
